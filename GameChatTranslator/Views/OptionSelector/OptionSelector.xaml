<Window x:Class="GameTranslator.OptionSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GameChatTranslator 환경 설정" Width="1300" Height="900"
        WindowStartupLocation="CenterScreen" Topmost="True" Background="#1E1E1E" Foreground="White" ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#8BE28B"/>
            <Setter Property="Margin" Value="0,16,0,6"/>
        </Style>
        <Style x:Key="SectionHintStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#AFAFAF"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <!-- 설정 항목을 3열로 배치해 Local LLM 추가 후 길어진 설정창을 더 짧고 넓게 정리합니다. -->
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="⚙️ 초기 환경 설정" FontSize="20" FontWeight="Bold" Foreground="LimeGreen" HorizontalAlignment="Center" Margin="0,0,0,6"/>
            <TextBlock Text="자주 쓰는 설정부터 번역 엔진 설정까지 순서대로 정리했습니다. 변경한 값은 아래 저장 버튼을 눌러야 적용됩니다."
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Foreground="#CCC"
                       Margin="0,0,0,14"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.95*"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="1.02*"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="1.23*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="1. 설정 관리" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBlock Text="프리셋, 설정 백업, 업데이트, 로그창처럼 프로그램 운영에 필요한 항목입니다." Style="{StaticResource SectionHintStyle}"/>

                    <GroupBox Header="사용자 프리셋 / 내장 추천 프리셋" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <TextBlock Text="내장 추천 프리셋"
                                       FontWeight="Bold"
                                       Foreground="#8FE388"
                                       Margin="0,0,0,4"/>
                            <TextBlock Text="저사양/빠름/정확도 버튼은 OCR 고급 설정 입력칸만 바꾸며 사용자 프리셋 목록에는 저장하지 않습니다. 적용 전 변경 예정값을 안내합니다."
                                       TextWrapping="Wrap"
                                       FontSize="11"
                                       Margin="0,0,0,6"
                                       Foreground="#AFAFAF"/>
                            <WrapPanel Margin="0,0,0,8">
                                <Button Content="저사양"
                                        Tag="low-spec"
                                        Padding="8,2"
                                        Margin="0,0,6,6"
                                        Background="#444"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Click="BtnApplyRecommendedPreset_Click"
                                        ToolTip="Scale 1 / Threshold 125 / 자동 번역 8초"/>
                                <Button Content="빠름"
                                        Tag="fast"
                                        Padding="8,2"
                                        Margin="0,0,6,6"
                                        Background="#444"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Click="BtnApplyRecommendedPreset_Click"
                                        ToolTip="Scale 2 / Threshold 120 / 자동 번역 2초"/>
                                <Button Content="정확도"
                                        Tag="accurate"
                                        Padding="8,2"
                                        Margin="0,0,6,6"
                                        Background="#444"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Click="BtnApplyRecommendedPreset_Click"
                                        ToolTip="Scale 4 / Threshold 115 / 누적 표시 10줄"/>
                            </WrapPanel>
                            <TextBlock x:Name="TxtRecommendedPresetStatus"
                                       Text="추천 프리셋을 누르면 현재값 → 추천값 차이를 표시합니다."
                                       TextWrapping="Wrap"
                                       FontSize="11"
                                       Margin="0,0,0,10"
                                       Foreground="#CCC"/>
                            <TextBlock Text="사용자 프리셋"
                                       FontWeight="Bold"
                                       Foreground="#8FE388"
                                       Margin="0,0,0,4"/>
                            <TextBlock Text="현재 설정을 이름별로 저장하고 다시 불러올 수 있습니다. API 키는 프리셋에 저장하지 않습니다."
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,8"
                                       Foreground="#CCC"/>
                            <ComboBox x:Name="ComboPreset" Height="24" Margin="0,0,0,8" Background="#333" Foreground="Black" SelectionChanged="ComboPreset_SelectionChanged"/>
                            <TextBox x:Name="TxtPresetName" Height="26" Margin="0,0,0,8" Background="#333" Foreground="White" BorderBrush="#555" ToolTip="저장할 프리셋 이름"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="BtnSavePreset" Content="프리셋 저장" Padding="8,2" Margin="0,0,8,0" Background="#444" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnSavePreset_Click"/>
                                <Button x:Name="BtnLoadPreset" Content="불러오기" Padding="8,2" Margin="0,0,8,0" Background="#444" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnLoadPreset_Click"/>
                                <Button x:Name="BtnDeletePreset" Content="삭제" Padding="8,2" Background="#444" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnDeletePreset_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="설정 백업 / 복원" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <TextBlock Text="현재 저장된 config.ini를 내보내거나, 백업한 config.ini를 가져옵니다. 프리셋도 함께 포함됩니다."
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,8"
                                       Foreground="#CCC"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="BtnExportSettings"
                                        Content="설정 내보내기"
                                        Padding="8,2"
                                        Margin="0,0,8,0"
                                        Background="#444"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Click="BtnExportSettings_Click"/>
                                <Button x:Name="BtnImportSettings"
                                        Content="설정 가져오기"
                                        Padding="8,2"
                                        Background="#444"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Click="BtnImportSettings_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="업데이트 확인 (Update)" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <CheckBox x:Name="CheckUpdatesOnStartup" Content="실행 시 업데이트 자동 확인" Foreground="#CCC" Margin="0,0,0,8"/>
                            <TextBlock Text="설치형(Setup.exe)으로 설치한 경우 새 버전을 앱에서 바로 다운로드/설치하고 재시작합니다. ZIP 직접 실행 버전은 릴리즈 페이지 안내를 유지합니다."
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,8"
                                       Foreground="#CCC"/>
                            <TextBlock Text="현재 실행 경로"
                                       Foreground="#8FE388"
                                       FontWeight="Bold"
                                       Margin="0,0,0,4"/>
                            <TextBlock x:Name="TxtInstallLocation"
                                       Text="경로 확인 중..."
                                       TextWrapping="Wrap"
                                       Foreground="#E6E6E6"
                                       FontFamily="Consolas"
                                       Margin="0,0,0,6"/>
                            <TextBlock Text="Setup.exe는 one-click 설치이며, 설치 경로를 직접 지정하려면 명령줄에서 --installto 옵션을 사용하세요."
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,8"
                                       Foreground="#AFAFAF"
                                       FontSize="11"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock x:Name="TxtUpdateStatus" Foreground="#CCC" FontSize="11" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <Button x:Name="BtnCheckUpdate" Content="업데이트 확인" Padding="8,2" Background="#444" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnCheckUpdate_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="로그 확인 (Log Viewer)" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <TextBlock Text="현재 세션 로그를 별도 창으로 열어 보조 모니터에서 실시간으로 확인할 수 있습니다." TextWrapping="Wrap" Margin="0,0,0,8" Foreground="#CCC"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="BtnOpenLogViewer" Content="로그창 열기" Padding="8,2" Background="#444" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnOpenLogViewer_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="2. 언어 / OCR / 실행" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBlock Text="게임 언어, OCR 상태, 진단, 실행 옵션을 한 열에서 조정합니다." Style="{StaticResource SectionHintStyle}"/>

                    <GroupBox Header="언어 설정 (Language)" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <TextBlock Text="게임 언어 (채팅 인식 기준):" Margin="0,0,0,5" Foreground="#CCC"/>
                            <ComboBox x:Name="ComboGameLang" Margin="0,0,0,10" Background="#333" Foreground="Black">
                                <ComboBoxItem Content="한국어" Tag="ko"/>
                                <ComboBoxItem Content="영어" Tag="en-US"/>
                                <ComboBoxItem Content="중국어(간체)" Tag="zh-Hans-CN"/>
                                <ComboBoxItem Content="일본어" Tag="ja"/>
                                <ComboBoxItem Content="러시아어" Tag="ru"/>
                            </ComboBox>

                            <TextBlock Text="번역 언어 (결과 언어):" Margin="0,0,0,5" Foreground="#CCC"/>
                            <ComboBox x:Name="ComboTargetLang" Background="#333" Foreground="Black">
                                <ComboBoxItem Content="한국어" Tag="ko"/>
                                <ComboBoxItem Content="영어" Tag="en-US"/>
                                <ComboBoxItem Content="중국어(간체)" Tag="zh-Hans-CN"/>
                                <ComboBoxItem Content="일본어" Tag="ja"/>
                                <ComboBoxItem Content="러시아어" Tag="ru"/>
                            </ComboBox>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="번역기 방식" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <RadioButton x:Name="RadioTranslationContentStrinova"
                                         Content="Strinova"
                                         Tag="Strinova"
                                         Foreground="#CCC"
                                         IsChecked="True"
                                         Margin="0,0,0,4"/>
                            <TextBlock Text="[캐릭터이름]: 채팅 형식과 characters.txt 캐릭터명 검증을 통과한 채팅만 번역합니다."
                                       TextWrapping="Wrap"
                                       FontSize="11"
                                       Foreground="#AFAFAF"
                                       Margin="22,0,0,8"/>
                            <RadioButton x:Name="RadioTranslationContentEtc"
                                         Content="ETC"
                                         Tag="ETC"
                                         Foreground="#CCC"
                                         Margin="0,0,0,4"/>
                            <TextBlock Text="채팅 포맷과 무관하게 OCR로 읽은 전체 내용을 하나의 문장 묶음으로 번역합니다."
                                       TextWrapping="Wrap"
                                       FontSize="11"
                                       Foreground="#AFAFAF"
                                       Margin="22,0,0,0"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="OCR 언어팩 설치 상태" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <TextBlock Text="기본은 OCR 엔진 사용 여부만 보여주고, capability는 재부팅이 필요해 보일 때만 경고로 덧붙입니다."
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,8"
                                       Foreground="#CCC"/>
                            <TextBlock x:Name="TxtOcrLanguageStatus"
                                       Text="OCR 언어팩 상태 확인 중..."
                                       TextWrapping="Wrap"
                                       FontFamily="Consolas"
                                       Foreground="#E6E6E6"
                                       Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="BtnRefreshOcrLanguages"
                                        Content="상태 새로고침"
                                        Padding="8,2"
                                        Background="#444"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Click="BtnRefreshOcrLanguages_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="OCR 테스트 / 진단" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <TextBlock Text="현재 저장된 캡처 영역을 분석해 원본, 전처리 후보, OCR 결과, 후보 점수를 별도 창에서 비교합니다."
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,8"
                                       Foreground="#CCC"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="BtnOpenOcrDiagnostic"
                                        Content="OCR 진단 화면 열기"
                                        Padding="8,2"
                                        Background="#444"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Click="BtnOpenOcrDiagnostic_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="상세 설정 (Advanced Options)" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">

                            <TextBlock x:Name="TxtOpacityInfo" Text="번역창 불투명도: 70%" Margin="0,0,0,5" Foreground="#CCC"/>
                            <Slider x:Name="SliderOpacity" Minimum="10" Maximum="100" Value="70" TickFrequency="5" IsSnapToTickEnabled="True" ValueChanged="SliderOpacity_ValueChanged" Margin="0,0,0,15"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="OCR 인식 배율 (1~4):" Width="180" VerticalAlignment="Center" Foreground="#CCC"/>
                                <ComboBox x:Name="ComboScale" Width="120" Height="24" Background="#333" Foreground="Black" ToolTip="기본값 3, 허용 범위 1~4">
                                    <ComboBoxItem Content="1배 (빠름)" Tag="1"/>
                                    <ComboBoxItem Content="2배" Tag="2"/>
                                    <ComboBoxItem Content="3배 (권장)" Tag="3"/>
                                    <ComboBoxItem Content="4배 (정밀)" Tag="4"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="OCR 이진화 기준 (1~255):" Width="180" VerticalAlignment="Center" Foreground="#CCC"/>
                                <TextBox x:Name="TxtThreshold" Width="120" TextAlignment="Center" Background="#333" Foreground="White" BorderBrush="#555" ToolTip="기본값 120, 권장 110~130" InputMethod.IsInputMethodEnabled="False" PreviewTextInput="NumericSetting_PreviewTextInput" TextChanged="NumericSetting_TextChanged"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="자동 번역 주기 (1~60초):" Width="180" VerticalAlignment="Center" Foreground="#CCC"/>
                                <TextBox x:Name="TxtInterval" Width="120" TextAlignment="Center" Background="#333" Foreground="White" BorderBrush="#555" ToolTip="기본값 5초, 허용 범위 1~60초" InputMethod.IsInputMethodEnabled="False" PreviewTextInput="NumericSetting_PreviewTextInput" TextChanged="NumericSetting_TextChanged"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="번역 결과 표시 방식:" Width="180" VerticalAlignment="Center" Foreground="#CCC"/>
                                <ComboBox x:Name="ComboResultDisplayMode" Width="170" Height="24" Background="#333" Foreground="Black">
                                    <ComboBoxItem Content="최신 결과만 표시" Tag="Latest"/>
                                    <ComboBoxItem Content="최근 내역 누적 표시" Tag="History"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="누적 표시 최대 줄 수 (1~10):" Width="180" VerticalAlignment="Center" Foreground="#CCC"/>
                                <TextBox x:Name="TxtResultHistoryLimit" Width="120" TextAlignment="Center" Background="#333" Foreground="White" BorderBrush="#555" ToolTip="기본값 5, 허용 범위 1~10" InputMethod.IsInputMethodEnabled="False" PreviewTextInput="NumericSetting_PreviewTextInput" TextChanged="NumericSetting_TextChanged"/>
                            </StackPanel>

                            <TextBlock x:Name="TxtAdvancedValidationStatus" TextWrapping="Wrap" FontSize="11" Foreground="#8BE28B" Margin="0,0,0,10"/>

                            <CheckBox x:Name="CheckSaveDebugImages" Content="디버그 캡처 이미지 저장" Foreground="#CCC"/>

                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="캡처 영역 (Capture Area)" Foreground="White" BorderBrush="#555" Margin="0,0,0,10">
                        <StackPanel Margin="10" Orientation="Horizontal">
                            <TextBlock x:Name="TxtAreaInfo" Text="저장된 영역: 없음 (좌측 하단 기본값)" VerticalAlignment="Center" Foreground="#CCC" Width="280"/>
                            <Button x:Name="BtnResetArea" Content="🔄 영역 초기화" MinWidth="118" Padding="10,0" Margin="12,0,0,0" Height="28" Background="#444" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnResetArea_Click"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="3. 단축키 / 번역 엔진" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBlock Text="기본 키 순서대로 단축키를 정리하고 Gemini / Local LLM 연결 정보를 입력합니다." Style="{StaticResource SectionHintStyle}"/>

                    <GroupBox Header="단축키 설정 (기본 키 순서)" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="Ctrl+5  OCR 진단 화면 :" Grid.Row="0" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="#CCC"/>
                                <TextBox x:Name="TxtKeyOcrDiagnostic" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5" PreviewKeyDown="Hotkey_PreviewKeyDown" IsReadOnly="True" Cursor="Pen" Background="#333" Foreground="White"/>

                                <TextBlock Text="Ctrl+6  번역 복사 :" Grid.Row="1" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="#CCC"/>
                                <TextBox x:Name="TxtKeyCopy" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5" PreviewKeyDown="Hotkey_PreviewKeyDown" IsReadOnly="True" Cursor="Pen" Background="#333" Foreground="White"/>

                                <TextBlock Text="Ctrl+7  이동/잠금 :" Grid.Row="2" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="#CCC"/>
                                <TextBox x:Name="TxtKeyMove" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5" PreviewKeyDown="Hotkey_PreviewKeyDown" IsReadOnly="True" Cursor="Pen" Background="#333" Foreground="White"/>

                                <TextBlock Text="Ctrl+8  영역 설정 :" Grid.Row="3" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="#CCC"/>
                                <TextBox x:Name="TxtKeyArea" Grid.Row="3" Grid.Column="1" Margin="0,0,0,5" PreviewKeyDown="Hotkey_PreviewKeyDown" IsReadOnly="True" Cursor="Pen" Background="#333" Foreground="White"/>

                                <TextBlock Text="Ctrl+9  수동 번역 :" Grid.Row="4" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="#CCC"/>
                                <TextBox x:Name="TxtKeyTrans" Grid.Row="4" Grid.Column="1" Margin="0,0,0,5" PreviewKeyDown="Hotkey_PreviewKeyDown" IsReadOnly="True" Cursor="Pen" Background="#333" Foreground="White"/>

                                <TextBlock Text="Ctrl+0  자동 번역 모드 :" Grid.Row="5" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="#CCC"/>
                                <TextBox x:Name="TxtKeyAuto" Grid.Row="5" Grid.Column="1" Margin="0,0,0,5" PreviewKeyDown="Hotkey_PreviewKeyDown" IsReadOnly="True" Cursor="Pen" Background="#333" Foreground="White"/>

                                <TextBlock Text="Ctrl+-  엔진 전환 :" Grid.Row="6" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="#CCC"/>
                                <TextBox x:Name="TxtKeyToggle" Grid.Row="6" Grid.Column="1" Margin="0,0,0,5" PreviewKeyDown="Hotkey_PreviewKeyDown" IsReadOnly="True" Cursor="Pen" Background="#333" Foreground="White"/>

                                <TextBlock Text="Ctrl+=  로그창 ON/OFF :" Grid.Row="7" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="#CCC"/>
                                <TextBox x:Name="TxtKeyLog" Grid.Row="7" Grid.Column="1" Margin="0,0,0,5" PreviewKeyDown="Hotkey_PreviewKeyDown" IsReadOnly="True" Cursor="Pen" Background="#333" Foreground="White"/>

                                <TextBlock Text="Ctrl+F10  단축키 안내 :" Grid.Row="8" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="#CCC"/>
                                <TextBox x:Name="TxtKeyHotkeyGuide" Grid.Row="8" Grid.Column="1" Margin="0,0,0,5" PreviewKeyDown="Hotkey_PreviewKeyDown" IsReadOnly="True" Cursor="Pen" Background="#333" Foreground="White"/>
                            </Grid>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
                                <Button x:Name="BtnResetHotkeys" Content="단축키 초기화" Padding="8,2" Background="#444" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnResetHotkeys_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="기본 번역 엔진" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <TextBlock Text="프로그램 시작 시 사용할 번역 엔진입니다. 실행 중에는 Ctrl+-로 순환 전환할 수 있습니다." TextWrapping="Wrap" Margin="0,0,0,8" Foreground="#CCC"/>
                            <ComboBox x:Name="ComboTranslationEngine" Height="24" Background="#333" Foreground="Black">
                                <ComboBoxItem Content="Google 무료 번역" Tag="Google"/>
                                <ComboBoxItem Content="Gemini AI 번역" Tag="Gemini"/>
                                <ComboBoxItem Content="Local LLM 번역" Tag="LocalLlm"/>
                            </ComboBox>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Gemini AI 번역 설정" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <TextBlock Text="API 키를 입력하면 Gemini AI 번역을 사용할 수 있습니다. 비워두면 Google 무료 번역으로 실행됩니다." TextWrapping="Wrap" Margin="0,0,0,10" Foreground="#CCC"/>

                            <TextBlock Text="Gemini API Key:" Margin="0,0,0,5" Foreground="#CCC"/>
                            <PasswordBox x:Name="PasswordGeminiKey" Height="26" Margin="0,0,0,10" Background="#333" Foreground="White" BorderBrush="#555"/>

                            <TextBlock Text="Gemini Model:" Margin="0,0,0,5" Foreground="#CCC"/>
                            <TextBox x:Name="TxtGeminiModel" Height="26" Margin="0,0,0,5" Background="#333" Foreground="White" BorderBrush="#555"/>
                            <TextBlock Text="기본값: gemini-2.5-flash" FontSize="11" Foreground="#888"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Local LLM 번역 설정 (실험)" Foreground="White" Margin="0,0,0,10" BorderBrush="#555">
                        <StackPanel Margin="10">
                            <TextBlock Text="LM Studio Local Server를 켠 뒤 OpenAI 호환 endpoint와 모델명을 입력합니다. 실패 시 Google 번역으로 전환됩니다." TextWrapping="Wrap" Margin="0,0,0,10" Foreground="#CCC"/>

                            <TextBlock Text="Local LLM Endpoint:" Margin="0,0,0,5" Foreground="#CCC"/>
                            <TextBox x:Name="TxtLocalLlmEndpoint" Height="26" Margin="0,0,0,5" Background="#333" Foreground="White" BorderBrush="#555"/>
                            <TextBlock Text="기본값: http://localhost:1234/v1/chat/completions" FontSize="11" Foreground="#888" TextWrapping="Wrap" Margin="0,0,0,8"/>

                            <TextBlock Text="Local LLM Model:" Margin="0,0,0,5" Foreground="#CCC"/>
                            <TextBox x:Name="TxtLocalLlmModel" Height="26" Margin="0,0,0,5" Background="#333" Foreground="White" BorderBrush="#555"/>
                            <TextBlock Text="권장 시작값: qwen/qwen3.5-9b" FontSize="11" Foreground="#888" TextWrapping="Wrap" Margin="0,0,0,8"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Timeout (초):" Margin="0,0,0,5" Foreground="#CCC"/>
                                    <TextBox x:Name="TxtLocalLlmTimeout" Height="26" Margin="0,0,0,5" Background="#333" Foreground="White" BorderBrush="#555"
                                             InputMethod.IsInputMethodEnabled="False" PreviewTextInput="NumericSetting_PreviewTextInput" TextChanged="NumericSetting_TextChanged"/>
                                    <TextBlock Text="1~60초, 기본 10초" FontSize="11" Foreground="#888"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Max Tokens:" Margin="0,0,0,5" Foreground="#CCC"/>
                                    <TextBox x:Name="TxtLocalLlmMaxTokens" Height="26" Margin="0,0,0,5" Background="#333" Foreground="White" BorderBrush="#555"
                                             InputMethod.IsInputMethodEnabled="False" PreviewTextInput="NumericSetting_PreviewTextInput" TextChanged="NumericSetting_TextChanged"/>
                                    <TextBlock Text="40~512, 기본 160" FontSize="11" Foreground="#888"/>
                                </StackPanel>
                            </Grid>

                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <Button x:Name="BtnTestLocalLlm" Content="Local LLM 연결 테스트" Padding="8,3" Background="#444" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnTestLocalLlm_Click"/>
                                <TextBlock x:Name="TxtLocalLlmTestStatus" Text="서버를 켠 뒤 연결 테스트를 눌러주세요." Margin="10,2,0,0" Foreground="#AAA" TextWrapping="Wrap" Width="280"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </Grid>

            <!-- 저장 버튼: 모든 설정을 config.ini에 기록하고 설정창을 정상 종료해 MainWindow 로딩을 계속합니다. -->
            <Button x:Name="BtnSaveAndStart" Content="💾 저장 및 게임 시작" Height="40" Background="LimeGreen" Foreground="Black" FontWeight="Bold" FontSize="16" Click="BtnSaveAndStart_Click" Cursor="Hand" Margin="0,8,0,10"/>
        </StackPanel>
    </ScrollViewer>
</Window>
