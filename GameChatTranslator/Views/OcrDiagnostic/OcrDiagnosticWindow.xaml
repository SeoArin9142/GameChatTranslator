<Window x:Class="GameTranslator.OcrDiagnosticWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GameChatTranslator OCR 테스트 / 진단"
        Width="1100"
        Height="780"
        MinWidth="760"
        MinHeight="520"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        Foreground="White"
        ResizeMode="CanResize"
        Topmost="True">
    <DockPanel Margin="12">
        <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button x:Name="BtnRunDiagnostic"
                        Content="현재 캡처 영역 진단"
                        Padding="12,5"
                        Margin="0,0,10,0"
                        Background="#444"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BtnRunDiagnostic_Click"/>
                <TextBlock x:Name="TxtStatus"
                           VerticalAlignment="Center"
                           Foreground="#8FE388"
                           FontWeight="Bold"
                           Text="진단 준비 완료"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="BtnRunHarness"
                        Content="선택 후보 번역 테스트"
                        Padding="12,5"
                        Margin="0,0,8,0"
                        Background="#5C4A7A"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        IsEnabled="False"
                        Click="BtnRunHarness_Click"/>
                <Button x:Name="BtnCopySummary"
                        Content="요약 복사"
                        Padding="12,5"
                        Margin="0,0,8,0"
                        Background="#444"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        IsEnabled="False"
                        Click="BtnCopySummary_Click"/>
                <Button x:Name="BtnCopyDiagnostic"
                        Content="전체 텍스트 복사"
                        Padding="12,5"
                        Margin="0,0,8,0"
                        Background="#444"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        IsEnabled="False"
                        Click="BtnCopyDiagnostic_Click"/>
                <Button x:Name="BtnSaveDiagnostic"
                        Content="ZIP 저장"
                        Padding="12,5"
                        Background="#3F5F7F"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        IsEnabled="False"
                        Click="BtnSaveDiagnostic_Click"/>
            </StackPanel>
        </Grid>

        <TextBlock DockPanel.Dock="Top"
                   Text="현재 저장된 채팅 캡처 영역을 실제 번역과 같은 기준으로 분석합니다. 요약 복사는 핵심 정보만, 전체 텍스트 복사는 후보별 OCR 원문까지, ZIP 저장은 이미지와 텍스트 전체를 묶어 저장합니다."
                   TextWrapping="Wrap"
                   Foreground="#CCC"
                   Margin="0,0,0,8"/>

        <Border DockPanel.Dock="Top"
                BorderBrush="#555"
                BorderThickness="1"
                Background="#252525"
                Padding="10"
                Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="선택 후보" Foreground="#AAAAAA" FontSize="11"/>
                    <TextBlock x:Name="TxtSelectedCandidate" Text="-" Foreground="White" FontWeight="Bold" FontSize="16"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="12,0,0,0">
                    <TextBlock Text="점수" Foreground="#AAAAAA" FontSize="11"/>
                    <TextBlock x:Name="TxtSelectedScore" Text="-" Foreground="#FFD966" FontWeight="Bold" FontSize="16"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="12,0,0,0">
                    <TextBlock Text="처리 시간" Foreground="#AAAAAA" FontSize="11"/>
                    <TextBlock x:Name="TxtTotalTime" Text="-" Foreground="#8FE388" FontWeight="Bold" FontSize="16"/>
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="12,0,0,0">
                    <TextBlock Text="OCR 호출" Foreground="#AAAAAA" FontSize="11"/>
                    <TextBlock x:Name="TxtOcrCalls" Text="-" Foreground="#9CDCFE" FontWeight="Bold" FontSize="16"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border DockPanel.Dock="Top"
                BorderBrush="#555"
                BorderThickness="1"
                Background="#252525"
                Padding="10"
                Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="TxtHarnessStatus"
                           Text="선택 후보 번역 테스트 준비"
                           Foreground="#CCCCCC"
                           FontWeight="Bold"
                           Margin="0,0,0,8"/>

                <TextBox x:Name="TxtHarnessPreview"
                         Grid.Row="1"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Background="#111111"
                         Foreground="#E6E6E6"
                         BorderBrush="#555555"
                         FontFamily="Consolas"
                         FontSize="12"
                         Padding="8"
                         MinHeight="160"
                         Text="선택 후보를 대상으로 [캐릭터이름]: 내용 형식을 우선 파싱하고, 내용만 현재 번역 엔진으로 테스트합니다."/>
            </Grid>
        </Border>

        <TabControl x:Name="TabDiagnostics"
                    Background="#222"
                    BorderBrush="#555"
                    Foreground="Black"/>
    </DockPanel>
</Window>
