<Window x:Class="GameTranslator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="테스트 번역기" 
        SizeToContent="Height" Width="400" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True"
        Loaded="Window_Loaded" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <!--
        MainWindow는 게임 화면 위에 뜨는 실제 번역 오버레이입니다.
        WindowStyle=None/AllowsTransparency=True로 일반 창 테두리를 제거하고,
        WindowUtils.SetClickThrough 호출 시 마우스 클릭이 게임으로 통과합니다.
    -->
    <Border x:Name="MainBorder" Background="#CC1E1E1E" CornerRadius="10" BorderThickness="2" BorderBrush="#55FFFFFF">
        <StackPanel Margin="15">
            <Grid Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 단축키와 현재 자동 번역 모드를 표시하는 상단 안내 영역입니다. MainWindow.Hotkeys.cs에서 동적으로 갱신합니다. -->
                <TextBlock x:Name="TxtHotkeyGuide"
                           Grid.Column="0"
                           Text="[Ctrl+0] 설정  [Ctrl+-] 번역  [Ctrl+=] 자동"
                           TextWrapping="Wrap"
                           Foreground="#FFCC00"
                           FontSize="11"
                           FontWeight="Bold"
                           LineHeight="16"
                           TextAlignment="Left"
                           HorizontalAlignment="Stretch"
                           Margin="0,0,8,0"/>

                <Button x:Name="BtnMoveLockToggle"
                        Grid.Column="1"
                        Content="위치 고정"
                        Padding="10,2"
                        MinWidth="74"
                        Height="24"
                        Background="#2F6B2F"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Visibility="Collapsed"
                        Click="BtnMoveLockToggle_Click"/>
            </Grid>

            <!-- 자동 번역 모드 전환 시 현재 상태를 잠깐 강조 표시합니다. 단축키 안내가 길어져도 전환 상태를 놓치지 않게 분리했습니다. -->
            <TextBlock x:Name="TxtAutoModeStatus"
                       TextWrapping="Wrap"
                       Foreground="#7CFF7C"
                       FontSize="12"
                       FontWeight="Bold"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"
                       Visibility="Collapsed"/>

            <!-- API 오류가 발생했을 때 게임 화면을 크게 가리지 않는 짧은 안내만 잠시 표시합니다. 상세 원인은 로그창에 남깁니다. -->
            <TextBlock x:Name="TxtApiStatus"
                       TextWrapping="Wrap"
                       Foreground="#FF9E64"
                       FontSize="12"
                       FontWeight="Bold"
                       LineHeight="17"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"
                       Visibility="Collapsed"/>

            <!-- 번역 결과 출력 영역입니다. 캐릭터명은 금색, 번역문은 흰색 Run으로 추가됩니다. -->
            <Border Background="#22FFFFFF" CornerRadius="5" Padding="10" MinHeight="100">
                <ScrollViewer x:Name="ResultScrollViewer"
                              MaxHeight="260"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <TextBlock x:Name="TxtResult" TextWrapping="Wrap" Foreground="White" FontSize="15" LineHeight="22">
                        번역 결과가 여기에 표시됩니다.
                    </TextBlock>
                </ScrollViewer>
            </Border>
        </StackPanel>
    </Border>
</Window>
